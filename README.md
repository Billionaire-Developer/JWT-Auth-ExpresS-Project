# 🔐 JWT Authentication API

A secure and scalable authentication API built with **Node.js**, **Express**, and **JSON Web Tokens (JWT)**.  
This project implements user registration, login, protected routes, and follows a clean MVC-style structure.

---

## 🚀 Features

- User registration with password hashing
- User login with JWT authentication
- Protected routes using JWT middleware
- Clean project structure (routes, controllers, middleware)
- Environment variable support with `.env`
- Ready for database integration (Prisma, Sequelize, MongoDB, etc.)

---

## 🧱 Tech Stack

- **Node.js**
- **Express.js**
- **JWT (jsonwebtoken)**
- **bcrypt**
- **dotenv**
- **CORS**

---
├── .env
└── .gitignore
